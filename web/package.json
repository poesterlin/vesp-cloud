{
  "name": "vesp-cloud",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @vesp-cloud/editor dev",
    "worker": "bun run --filter @vesp-cloud/editor worker",
    "dev:admin": "bun run --filter @vesp-cloud/admin dev",
    "build": "bun run generate:types && bun run --filter @vesp-cloud/editor build",
    "build:admin": "bun run --filter @vesp-cloud/admin build",
    "generate:types": "bun run --filter @vesp-cloud/schema generate",
    "preview": "bun run --filter @vesp-cloud/editor preview",
    "check": "bun run --filter @vesp-cloud/editor check",
    "lint": "bun run --filter '*' lint",
    "test": "bun run --filter @vesp-cloud/editor test",
    "test:watch": "bun run --filter @vesp-cloud/editor test:watch",
    "test:codegen": "bun run --filter @vesp-cloud/editor test:codegen",
    "db:generate": "cd packages/editor && bun run db:generate",
    "db:push": "cd packages/editor && bun run db:push"
  },
  "devDependencies": {
    "@mdi/js": "7.4.47",
    "@types/bun": "latest",
    "typescript": "5.9.3",
    "vite-plugin-devtools-json": "1.0.0"
  },
  "module": "index.ts",
  "type": "module"
}
